a=DB.user;
Print(a&Line);
DB.user=`acccess`;
a=DB.user;
Print(a&Line);
Print(DB.user&Space&DB.password&Line);
BACKUP=`files`;
BACKUP.files=`*.png`;
BACKUP.myname=`donut`;
Print(BACKUP&Space&BACKUP.files&Space&BACKUP.myname&Line);
